<?xml version="1.0" encoding="utf-8"?>
<resources>

    <color name="brand">@color/dodge</color>
    <color name="brand_inverse">@color/white</color>
    <color name="accent">@color/dodge</color>
    <color name="accent_inverse">@color/white</color>

    <color name="background">@color/cod</color>
    <color name="background_inverse">@color/white</color>

    <color name="surface">@color/cod</color>
    <color name="surface_inverse">@color/white</color>
    <color name="surface_secondary">@color/mine</color>
    <color name="surface_secondary_inverse">@color/white</color>

    <color name="control">@color/white</color>
    <color name="control_activated">@color/brand</color>
    <color name="control_highlight">@color/brand</color>

    <color name="text_primary">@color/white</color>
    <color name="text_primary_inverse">@color/cod</color>
    <color name="text_secondary">@color/dusty</color>
    <color name="text_secondary_inverse">@color/cod</color>
    <color name="text_error">@color/danger</color>
    <color name="text_error_inverse">@color/white</color>

    <!-- Number value indicators -->
    <color name="positive">@color/green</color>
    <color name="negative">@color/danger</color>

    <!-- Components -->
    <!-- Main buttons -->
    <color name="button_primary">@color/white</color>
    <color name="button_secondary">@color/transparent</color>

    <!-- TextIcon button -->
    <color name="button_texticon">@color/mine</color>

    <!-- Suggestion button -->
    <color name="button_suggestion">@color/venus</color>

    <!-- Icons, toolbar icons -->
    <color name="icon_tint">@color/white</color>
    <color name="icon_tint_inverse">@color/cod</color>

    <!-- Warning dialog -->
    <color name="warning_background">@color/mine</color>

    <!-- Headers -->
    <color name="header_background">@color/venus</color>

    <!-- Separator -->
    <color name="separator">@color/venus</color>

    <!-- Address Bar -->
    <color name="search_bar">@color/mine</color>

    <!-- History Chart -->
    <color name="chart_tab">@color/cod</color>
    <color name="chart_tab_selected">@color/white</color>
    <color name="chart_legend">@color/white</color>
    <color name="chart_line">@color/white</color>

    <!-- Seed Word -->
    <color name="seed_word">@color/mine</color>
    <color name="seed_word_selected">@color/venus</color>
    <color name="seed_word_stroke">@color/mike</color>
    <color name="seed_word_text">@color/text_primary</color>
    <color name="seed_word_text_selected">@color/venus</color>

</resources>
