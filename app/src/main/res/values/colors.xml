<?xml version="1.0" encoding="utf-8"?>
<resources>
	<color name="background">@color/white</color>
	<color name="background_inverse">@color/black</color>

	<color name="surface">@color/white</color>
	<color name="surface_inverse">@color/black</color>

	<color name="control">@color/black</color>
	<color name="control_activated">@color/brand</color>

	<color name="brand">@color/azure</color> <!-- TODO: Make fixed -->
	<color name="brand_inverse">@color/white</color> <!-- TODO: Make fixed -->

	<color name="accent">@color/azure</color> <!-- TODO: Make fixed -->
	<color name="accent_inverse">@color/white</color> <!-- TODO: Make fixed -->

	<!-- Most prominent text color -->
	<color name="text_primary">@color/black</color>
	<color name="text_primary_inverse">@color/white</color>

	<!-- Secondary text color for subtitles -->
	<color name="text_secondary">@color/dove</color>
	<color name="text_secondary_inverse">@color/white</color>

	<!-- Error colors -->
	<color name="text_error">@color/danger</color> <!-- TODO: Make fixed -->
	<color name="text_error_inverse">@color/white</color> <!-- TODO: Make fixed -->

	<!-- Main buttons -->
	<color name="button_primary">@color/cod</color>
	<color name="button_secondary">@color/transparent</color>

	<!-- TextIcon button -->
	<color name="button_texticon">@color/concrete</color>

	<!-- Suggestion button -->
	<color name="button_suggestion">@color/mercury</color>

	<!-- Icons, toolbar icons -->
	<color name="icon_tint">@color/black</color>
	<color name="icon_tint_inverse">@color/white</color>

	<!-- Popup menus, dialogs -->
	<color name="dialog_background">@color/white</color> <!-- TODO: Make fixed -->

	<!-- Warning dialog -->
	<color name="warning_background">@color/mine</color> <!-- TODO: Make fixed -->

	<!-- Headers -->
	<color name="header_background">@color/alabaster</color>

	<!-- Separator -->
	<color name="separator">@color/mercury</color>

	<!-- Address Bar -->
	<color name="search_bar">@color/concrete</color>

	<!-- History Chart -->
	<color name="chart_tab">@color/white</color>
	<color name="chart_tab_selected">@color/mine</color>
	<color name="chart_legend">@color/black</color>
	<color name="chart_line">@color/mine</color>

	<!-- Seed Word -->
	<color name="seed_word">@color/concrete</color>
	<color name="seed_word_selected">@color/alabaster</color>
	<color name="seed_word_stroke">@color/mike</color> <!-- TODO: Fixed -->
	<color name="seed_word_text">@color/text_primary</color>
	<color name="seed_word_text_selected">@color/alabaster</color>

	<!-- Number value indicators -->
	<color name="positive">@color/green</color> <!-- TODO: Fixed -->
	<color name="negative">@color/danger</color> <!-- TODO: Fixed -->
</resources>
